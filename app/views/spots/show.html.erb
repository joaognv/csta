

<div class="subpage_banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.3) 0%, rgba(0,36,61,0.4) 50%), url(<%= asset_path 'socks_feet' %>);">
  <div>
    <h1>Your Chill Spot</h1>


  </div>
</div>

<div class="spot_wrapper">
  <div class="bg-img" style="background-image: url(<%= @spot.photo.url ? @spot.photo.url : "http://www.muebles.com/wp-content/uploads/2013/02/Ambiente-Terraza-Rattan-sintetico.jpg";  %>);">
  </div>
      <div>
<!--       <h3><%= @spot.address %>
      </h3>
      <p><%= @spot.description %> -->
    </div>
  <div class="spot-details">

    <div class="booking-form">
        <%= simple_form_for([@spot, @booking]) do |f| %>
        <%= f.error_notification %>

        <div class="form-inputs">
          <%= f.input :time_arrive, as: :date %>
          <%= f.input :time_leave, as: :date %>
        </div>

        <div class="form-actions">
          <%= f.button :submit %>
        </div>
      <% end %>

   </div>
  </div>
</div>




<div class="container">

<p> <%=  @spot.description %></p>

<%= link_to 'Back to spots', spots_path, class: "btn btn-primary" %>

</div>
